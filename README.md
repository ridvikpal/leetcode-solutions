# LeetCode Solutions

This repo contains solutions to [the LeetCode problems I've solved](https://leetcode.com/u/RP2426/), and problems from related platforms such as NeetCode and HackerRank.

The total count of the number of solutions in this repo can be found using the following bash command:

```bash
find . -type f -not -path "./.git/*" -not -path "./README.md" | wc -l
```
